# leslie-quickstart-registry

The shared Agent Registry for the Leslie quickstart demo rig (ADR-0003):
Agent Definitions live under `agents/<name>.md`, versions are git tags
`<name>/v<version>`, and every supervised Project's daemon syncs this repo
read-only each Tick.
